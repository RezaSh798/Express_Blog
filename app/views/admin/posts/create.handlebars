<div class="row">

    <!--Form controls Start-->
    <div class="col-12 mb-30">
        <div class="box">
            <div class="box-head">
                <h3 class="title">ایجاد مطلب جدید</h3>
            </div>
            <div class="box-body">
                <div class="row mbn-20">
                    {{#if hasError}}
                    {{#each errors}}
                    <div class="alert alert-danger ml-1" role="alert">
                        {{ this }}
                    </div>
                    {{/each}}
                    {{/if}}
                    <form action="/admin/posts/store" method="post">
                        <!--Form Field-->
                        <div class="">
                            <div class="row mbn-15">
                                <div class="col-12 mb-15"><input name="title" type="text" class="form-control" placeholder="عنوان مطلب"></div>
                                <div class="col-12 mb-15"><input name="slug" type="text" class="form-control" placeholder="نامک مطلب"></div>
                                <div class="col-12 mb-15"><textarea name="content" class="form-control" placeholder="منطقه متن"></textarea></div>
                                <div class="col-12 mb-15">
                                    <select class="form-control" name="author_id">
                                        {{#each users}}
                                        <option value="{{ this.id }}">{{ this.full_name }}</option>
                                        {{/each}}
                                    </select>
                                </div>
                            </div>

                            <!--Radio-->
                            <div class="col-lg-4 col-12 mb-20">
                                <label><input type="radio" name="status" value="0">پیش نویس</label>
                                <label><input type="radio" name="status" value="1">درحال بررسی</label>
                                <label><input type="radio" name="status" value="2">منتشر شده</label>
                            </div>
                            <!--Radio-->

                            <button type="submit" class="button button-primary"><span>ذخیره</span></button>

                        </div>
                        <!--Form Field-->
                    </form>
                </div>
            </div>
        </div>
    </div>
    <!--Form controls End-->
</div>